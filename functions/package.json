{
  "name": "forma-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "mkdir -p prompts lib/prompts && cp ../shared/prompts/*.txt prompts/ && tsc && cp prompts/*.txt lib/prompts/",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
