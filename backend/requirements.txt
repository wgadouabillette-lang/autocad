# API légère — utilisée par Vercel en production (connecteurs, billing, auth).
# En local, ajoutez aussi : pip install -r requirements-cad.txt
#
# --- API ---
fastapi>=0.115,<0.130
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# --- Connecteurs / auth / billing ---
httpx>=0.27
python-dotenv>=1.0
firebase-admin>=6.6
stripe>=11,<14
google-cloud-secret-manager>=2.20
